import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <h1>🚧Site actuellement en construction !🚧</h1>;

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
